# Core dependencies
jsonschema>=4.23,<5
fastmcp>=2.0,<4
PyMuPDF>=1.24.0,<2
requests>=2.28,<3

# Academic research dependencies (optional: pip install -e .[academic])
# PyYAML>=6.0

# Optional: Semantic retrieval with local embedding model
# pip install -e .[embedding]
# sentence-transformers>=3.0
